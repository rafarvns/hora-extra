# ── Hardening de supply chain ───────────────────────────────────────────────
# Contexto e racional completos: docs/Infrastructure/SUPPLY-CHAIN.md

# Pina versões EXATAS ao adicionar novas deps (`npm install <pkg>` grava "1.2.3",
# não "^1.2.3"). Fecha a janela de re-resolução que worms tipo Shai-Hulud/Miasma
# exploram. As deps já existentes continuam travadas pelo package-lock.json.
save-exact=true

# Reduz ruído de saída (mensagens de funding).
fund=false
